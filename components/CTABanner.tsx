import Link from "next/link";
import { ArrowTopRightOnSquareIcon } from "@heroicons/react/24/outline";
type Props = { eyebrow?: string; title: string; text: string; button: string; href: string; variant?: "blue" | "orange" | "light" };
const variants = { blue: "bg-brand-700 text-white", orange: "bg-gradient-to-br from-accent-500 to-orange-600 text-white", light: "border border-slate-200 bg-white text-slate-900" };
export default function CTABanner({ eyebrow, title, text, button, href, variant = "blue" }: Props) { return <div className={`rounded-2xl p-6 shadow-soft ${variants[variant]}`}><p className={`text-xs font-bold uppercase tracking-[.18em] ${variant === "light" ? "text-accent-600" : "text-white/70"}`}>{eyebrow}</p><h3 className="mt-2 text-xl font-bold">{title}</h3><p className={`mt-2 text-sm leading-6 ${variant === "light" ? "text-slate-600" : "text-white/80"}`}>{text}</p><Link href={href} target="_blank" rel="sponsored nofollow" className={`mt-5 inline-flex items-center gap-2 rounded-xl px-4 py-3 font-bold transition hover:-translate-y-0.5 ${variant === "light" ? "bg-accent-500 text-white" : "bg-white text-brand-900"}`}>{button}<ArrowTopRightOnSquareIcon className="h-4 w-4" /></Link></div>; }
