import Link from "next/link";
import { ArrowRightIcon, CalendarDaysIcon } from "@heroicons/react/24/outline";
import type { Article } from "@/data/articles";
export default function ArticleCard({ article }: { article: Article }) { return <article className="group overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm transition hover:-translate-y-1 hover:shadow-soft"><div className="relative flex aspect-[16/9] items-end overflow-hidden bg-gradient-to-br from-brand-700 via-brand-500 to-accent-500 p-5"><div className="absolute inset-0 bg-hero-grid bg-[size:20px_20px] opacity-40" /><span className="relative rounded-full bg-white/15 px-3 py-1 text-xs font-bold text-white backdrop-blur">{article.category}</span></div><div className="p-6"><p className="flex items-center gap-2 text-sm text-slate-500"><CalendarDaysIcon className="h-4 w-4" />{article.date}</p><h2 className="mt-3 text-xl font-bold leading-snug text-brand-900 group-hover:text-brand-700">{article.title}</h2><p className="mt-3 line-clamp-3 leading-6 text-slate-600">{article.excerpt}</p><Link href={`/blog/${article.slug}`} className="mt-5 inline-flex items-center gap-2 font-bold text-accent-600">Ler mais <ArrowRightIcon className="h-4 w-4 transition group-hover:translate-x-1" /></Link></div></article>; }
