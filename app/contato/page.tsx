"use client";
import { FormEvent, useState } from "react";
export default function Contato(){const [errors,setErrors]=useState<Record<string,string>>({});const [success,setSuccess]=useState(false);function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const fd=new FormData(e.currentTarget);const next:Record<string,string>={};["nome","email","assunto","mensagem"].forEach(k=>{if(!String(fd.get(k)||"").trim())next[k]="Campo obrigatório."});const email=String(fd.get("email")||"");if(email&&!/^\S+@\S+\.\S+$/.test(email))next.email="Informe um e-mail válido.";setErrors(next);if(!Object.keys(next).length){setSuccess(true);e.currentTarget.reset();}}return <section className="container-site py-16"><div className="mx-auto max-w-3xl"><p className="font-bold uppercase tracking-widest text-accent-600">Fale conosco</p><h1 className="section-title mt-3">Contato</h1><p className="mt-4 text-slate-600">Envie sua dúvida, sugestão ou mensagem. Não temos acesso físico às encomendas.</p><form onSubmit={submit} className="card mt-10 space-y-6 p-7 sm:p-10" noValidate>{success&&<div className="rounded-xl border border-emerald-200 bg-emerald-50 p-4 font-medium text-emerald-800" role="status">Mensagem enviada com sucesso! Esta é uma simulação do formulário.</div>}{[["nome","Nome","text"],["email","E-mail","email"],["assunto","Assunto","text"]].map(([id,label,type])=><div key={id}><label htmlFor={id} className="font-semibold text-slate-800">{label}</label><input id={id} name={id} type={type} className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3 focus:border-brand-500 focus:ring-4 focus:ring-blue-100" aria-invalid={!!errors[id]}/>{errors[id]&&<p className="mt-1 text-sm text-red-600">{errors[id]}</p>}</div>)}<div><label htmlFor="mensagem" className="font-semibold text-slate-800">Mensagem</label><textarea id="mensagem" name="mensagem" rows={6} className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3 focus:border-brand-500 focus:ring-4 focus:ring-blue-100" aria-invalid={!!errors.mensagem}/>{errors.mensagem&&<p className="mt-1 text-sm text-red-600">{errors.mensagem}</p>}</div><button className="btn-primary w-full sm:w-auto" type="submit">Enviar mensagem</button></form></div></section>}
